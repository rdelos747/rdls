// import React, { createContext, useEffect, useState } from 'react';

// const AppContext = createContext({});

// export const AppProvider = ({ children }) => {
//   const [appColor, setAppColor] = useState([255, 255, 255]);

//   useEffect(() => {
//     setAppColor([255, 0, 0]);
//   }, []);

//   return <AppContext.Provider value={{ color: appColor }}>{children}</AppContext.Provider>;
// };

// export default AppContext;
